version: '3.8'
services:
  ozen:
    image: ozen:latest
    container_name: ozen
    environment:
      - KAFKA_BROKERS=partzoof.apps.shtoot.oraclevcn.com:9092
      - PORT=4000
    ports:
      - "4000:4000"
