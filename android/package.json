{
  "name": "shtoot-android",
  "version": "0.1.0",
  "private": true,
  "description": "Android app for Shtoot, wrapping the peh frontend via Capacitor.",
  "scripts": {
    "sync-peh": "node scripts/sync-peh.js",
    "sync": "npm run sync-peh && npx cap sync android",
    "open": "npx cap open android",
    "run:android": "npm run sync && npx cap run android",
    "build:android": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0",
    "@codetrix-studio/capacitor-google-auth": "^3.4.0-rc.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
